# XcodeGen tarafından üretilir, git'te tutulmaz
VerifyBlind.xcodeproj/
*.xcworkspace/

# Xcode build artifactları
build/
DerivedData/
.build/
*.ipa
*.dSYM
*.dSYM.zip
*.xcarchive

# CocoaPods (yalnız ML Kit — bkz. Podfile). Pods/ `pod install` ile yeniden üretilir.
# ⚠️ Podfile.lock BURAYA EKLENMEZ: sürüm kilidi odur, commit edilmesi gerekir.
Pods/

# SPM
.swiftpm/
Packages/
Package.pins
# Package.resolved: reproducible build için commit edilebilir (Mac/CI ürettiğinde).
# Sürüm pin'i şu an project.yml'de exactVersion ile sağlanıyor.

# macOS
.DS_Store

# Xcode user-specific state
xcuserdata/
*.xcuserstate

# Local env override (xcconfig CI'da env'den üretilir; yanlışlıkla secret commit'i engelle)
Config/Debug.local.xcconfig
Config/Release.local.xcconfig

# OS
.DS_Store

# pymobiledevice3/go-ios eslesme kimligi — OZEL ANAHTAR icerir, asla commit edilmez
selfIdentity.plist
